import React from 'react';
import { VerticalExtensionManager } from '@/components/extensions/VerticalExtensionManager';

const Extensions: React.FC = () => {
  return (
    <div className="h-full">
      <VerticalExtensionManager />
    </div>
  );
};

export default Extensions;
